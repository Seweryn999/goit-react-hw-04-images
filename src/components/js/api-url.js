export const apiUrl = (querry, page, perPage) => {
  let url = `?key=34758818-84286f7512264df00409bd0b7&q=${querry}&image_type=photo&orientation=horizontal&page=${page}&per_page=${perPage}`;
  return url;
};
